#!/bin/bash
if [ "$(id -u)" != "0" ]; then
   echo "This script must be run as root" 1>&2
   exit 1
fi
cd /var/www/repos/apt/ubuntu
rm -rf db/ dists/ pool/
find <%= debpath %> -name "*.deb" -exec reprepro -Vb . includedeb precise '{}' ';'